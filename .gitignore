node_modules/

# Hardhat
packages/contracts/artifacts/
packages/contracts/cache/

# prover-gnark: large/regenerable artifacts (rebuild via cmd/setup + scripts/build-ios.sh).
# Kept (small, source of truth): build/Verifier.sol, keys/vk.bin
packages/prover-gnark/keys/pk.bin
packages/prover-gnark/keys/circuit.r1cs
packages/prover-gnark/build/Cloister.xcframework/
packages/prover-gnark/build/*.wasm
packages/prover-gnark/proverd

# web demo: built assets + large prover artifacts (regenerable)
apps/web/dist/
apps/web/public/gnark/

# TypeScript incremental build cache (regenerable, churns every build)
*.tsbuildinfo

*.log
.DS_Store
.env
.env.*

# deployment outputs (per-chain, regenerated by deploy)
deployment.*.json

# Deploy build output (coming-soon is source; deploy/ is generated)
/deploy/
