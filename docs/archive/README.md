# Archived docs (superseded)

These are creation-phase / pre-gnark-rewrite documents (mostly German), kept for history.
They describe an earlier design (circom/snarkjs/WebView, BabyJubJub) and do NOT reflect the
as-built system. The maintained, authoritative docs live in `docs/en/`.
