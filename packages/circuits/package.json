{
  "name": "@ocp-shield/circuits",
  "version": "0.1.0",
  "private": true,
  "description": "Groth16 circuits for the OCP-Shield shielded pool (transact UTXO)",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "rm -rf build"
  },
  "dependencies": {
    "circomlib": "^2.0.5"
  },
  "devDependencies": {
    "circomlibjs": "0.1.7",
    "ffjavascript": "^0.3.1",
    "snarkjs": "^0.7.5"
  }
}
