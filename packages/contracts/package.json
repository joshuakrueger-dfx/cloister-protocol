{
  "name": "@cloister/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shielded pool contracts (pool, verifier, registry) for Cloister",
  "exports": {
    "./deploy": "./src/deploy.mjs"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node"
  },
  "dependencies": {
    "@cloister/sdk": "workspace:*",
    "@openzeppelin/contracts": "^5.1.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.10"
  }
}
